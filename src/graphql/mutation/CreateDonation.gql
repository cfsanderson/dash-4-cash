# given a group id, charity, amount, and user id - it creates a donation in app

mutation CreateDonation ($ownerId: ID!, $name: String!) {
  createGroup(ownerId: $ownerId, name: $name) {
    id
    name
  }
}
